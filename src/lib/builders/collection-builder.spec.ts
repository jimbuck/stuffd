import { test } from 'ava';
import { CollectionBuilder  } from './collection-builder';

test.todo('CollectionBuilder#using stores the explicit property options');

test.todo('CollectionBuilder#cross stores the crossed properties');

test.todo('CollectionBuilder#cross can only be called once');

test.todo('CollectionBuilder#create requires count or cross');

test.todo('CollectionBuilder#create returns exact count for non-crossed');

test.todo('CollectionBuilder#create ignores count for crossed');